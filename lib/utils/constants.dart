// button animation duration
const Duration kButtonAnimationDuration = Duration(milliseconds: 300);

// text animation duration
const Duration kTextAnimationDuration = Duration(milliseconds: 300);

// profile photo animation duration
const Duration kProfilePhotoAnimationDuration = Duration(milliseconds: 50);
